Para usar las dependencias de un proyecto anterior debemos usar el `package.json` del proyecto que queramos replicar.

Para crear un nuevo proyecto, deberemos estar dentro de la carpeta donde se vaya a almacenar el proyecto:

``` Bash
npm init
```

Esto sirve para mover/copiar un proyecto con el fichero `package.json`:

``` Bash
npm install
```