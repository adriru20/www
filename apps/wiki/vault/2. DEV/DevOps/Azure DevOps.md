### Git
Para poder hacer commits's abrimos un terminal en VS Code y ponemos esto:
``` PowerShell
git config --global user.name "globaladmin"
git config --global user.email "adminglobal@adriru2004gmail.onmicrosoft.com"
```
Y para ver lo que está configurado usamos:
``` PowerShell
git config --list
```
