Expresiones: `{{ variable }}` --> `{{ ansible_fqdn }}`
Instrucción de control: `{% ... %}`
- Condición:
	``` yaml
	{% if ansible_distribution == "Debian" %}
	```
	Mostrar solo en sistemas Debian.
	``` yaml
	{% endif %}
	```

Fichero /roles/apache2/templates/index.html.j2:
```
<style>*{margin:5px;} h1{text-align: center;}</style>
{% if ansible_distribution == "Ubuntu" %}
<h1>El servidor {{ ansible_fqdn }} usa {{ ansible_distribution }}.</h1>
{% endif %}
<h2>Lista de usuarios:</h2>
<br/>
<ol>
{% for usuario in lista_usuarios %}
<li>{{ usuario }}</li>
{% endfor %}
</ol>
```

``` yaml
--- #roles.yml
- name: Instalar y configurar apache2
  hosts: python
  become: true
  roles:
    # - apache2 #Esto nos servirá para ejecutar el rol de apache2 con los ficheros por separado
    - { role: apache2, lista_usuarios: ["root","ansible"]} #También podemos especificar alguna variable directamente aquí
```